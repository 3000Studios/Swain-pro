# Cloudflare Pages secrets — set these in the CF dashboard or via wrangler secret put
CONTACT_EMAIL=Mr.Jwswain@gmail.com
OPENAI_API_KEY=sk-...

# Public (browser-safe) build-time vars — optional overrides.
# Defaults are baked into BaseLayout; set these only to point at different accounts.
# AdSense publisher id is already public via /ads.txt, so it is NOT a secret.
PUBLIC_GA_MEASUREMENT_ID=G-DH1WBK8YWK
PUBLIC_ADSENSE_CLIENT=ca-pub-5800977493749262
